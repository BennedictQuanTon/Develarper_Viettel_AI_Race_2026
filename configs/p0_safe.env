# P0 — first Hub submit (close to BTC sample + prefix caching)
MODEL_PATH=/model
SERVED_MODEL_NAME=LFM2.5-1.2B-Instruct
HOST=0.0.0.0
PORT=8000

MAX_MODEL_LEN=32768
GPU_MEMORY_UTILIZATION=0.95
TENSOR_PARALLEL_SIZE=1

ENABLE_PREFIX_CACHING=1
ENABLE_CHUNKED_PREFILL=0

# P0: no KV-FP8 / no speculative until baseline ERS exists
KV_CACHE_DTYPE=
QUANTIZATION=
