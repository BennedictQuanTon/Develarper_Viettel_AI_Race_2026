# P1 — Aggressive (only after P0 has a real ERS score)
# Change ONE variable per online submission when ablating.

MODEL_PATH=/model_weights/LLM-FP8
HOST=0.0.0.0
PORT=8000

GPU_MEMORY_UTILIZATION=0.92
MAX_MODEL_LEN=32768
MAX_NUM_BATCHED_TOKENS=8192

ENABLE_PREFIX_CACHING=1
ENABLE_CHUNKED_PREFILL=1

# A/B candidates — enable intentionally
KV_CACHE_DTYPE=fp8
# Speculative: only if official draft exists for the BTC model
# SPECULATIVE_CONFIG={"method":"eagle3","model":"/model_weights/EAGLE3-Draft","num_speculative_tokens":2}
SPECULATIVE_CONFIG=
