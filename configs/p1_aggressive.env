# P1 — change ONE knob per online submission
MODEL_PATH=/model
SERVED_MODEL_NAME=LFM2.5-1.2B-Instruct
HOST=0.0.0.0
PORT=8000

MAX_MODEL_LEN=32768
GPU_MEMORY_UTILIZATION=0.95
TENSOR_PARALLEL_SIZE=1
MAX_NUM_BATCHED_TOKENS=4096

ENABLE_PREFIX_CACHING=1
ENABLE_CHUNKED_PREFILL=1

# Candidates (enable intentionally during ablation)
KV_CACHE_DTYPE=
QUANTIZATION=
# e.g. QUANTIZATION=fp8
