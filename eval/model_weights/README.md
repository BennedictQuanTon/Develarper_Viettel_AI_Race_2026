# Model weights directory

Competition model: **LiquidAI/LFM2.5-1.2B-Instruct**

Preferred path: bake into the Docker image at **`/model`** during `docker build` (see root `Dockerfile`).

This folder is optional for local mounts during dev. Do **not** commit real weight files.
